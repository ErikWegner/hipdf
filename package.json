{
  "private": true,
  "version": "0.0.0-development",
  "license": "MIT",
  "scripts": {
    "test": "cargo test",
    "build": "cargo build",
    "test:wasm": "wasm-bindgen-test --chrome --headless",
    "test:wasm:firefox": "wasm-bindgen-test --firefox --headless",
    "test:wasm:safari": "wasm-bindgen-test --safari --headless",
    "test:all": "npm run test && npm run test:wasm"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "13.0.1",
    "@semantic-release/exec": "7.1.0",
    "@semantic-release/release-notes-generator": "14.0.3",
    "semantic-release": "24.2.7"
  },
  "dependencies": {
    "@semantic-release/github": "^11.0.5"
  }
}